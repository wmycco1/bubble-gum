# 🚀 Quick Start: Dev Server Management

## 🔴 Проблема

Команда `pkill -9 node` убивает **ВСЕ** Node процессы, включая:
- ❌ VSCode (Electron/Node.js)
- ❌ Claude Code CLI
- Результат: VSCode падает, показывает "Restart", теряется сессия

---

## ✅ Безопасное решение

### Вариант 1: Скрипт (самый простой)

```bash
./KILL_DEV_SERVER.sh
npm run dev
```

### Вариант 2: Одна команда

```bash
lsof -ti:3000,3010 | xargs kill -9 2>/dev/null && pkill -9 -f "next-server" 2>/dev/null && pkill -9 -f "turbopack" 2>/dev/null && rm -rf .next && sleep 2 && npm run dev
```

### Вариант 3: Aliases (самый удобный)

1. Установи aliases:
```bash
cat BASH_ALIASES.sh >> ~/.bashrc
source ~/.bashrc
```

2. Используй:
```bash
kill-bg         # Убить + очистить кэш
restart-bg      # Рестарт одной командой
start-bg        # Перейти в проект → убить → запустить
```

---

## 📖 Полная документация

[docs/DEV_SERVER_MANAGEMENT.md](./docs/DEV_SERVER_MANAGEMENT.md)

---

## 🎯 Быстрая справка

| Команда | Что делает |
|---------|------------|
| `./KILL_DEV_SERVER.sh` | Убить dev сервер + очистить кэш |
| `kill-bg` (alias) | То же самое через alias |
| `restart-bg` (alias) | Убить + запустить заново |
| `start-bg` (alias) | cd в проект + убить + запустить |

---

## ⚠️ НЕ используй

```bash
# ❌ ОПАСНО: Убьёт VSCode!
pkill -9 node

# ❌ ОПАСНО: Убьёт VSCode!
ps aux | grep -E "npm|node|next" | grep -v grep | awk '{print $2}' | xargs kill -9
```

---

**Версия:** 1.0.0
**Дата:** November 10, 2025
