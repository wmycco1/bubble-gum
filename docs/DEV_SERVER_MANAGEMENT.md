# Dev Server Management Guide

**Версия:** 1.2.0
**Дата создания:** November 10, 2025
**Последнее обновление:** November 11, 2025
**Статус:** ✅ Active
**Назначение:** Правила управления dev сервером для предотвращения проблем с портами и кэшем

---

## 🚨 КРИТИЧНО: ТОЛЬКО ПОРТ 3000!

**СТРОГО ЗАПРЕЩЕНО запускать dev сервер на других портах!**

- ✅ **РАЗРЕШЕН:** `PORT=3000 npm run dev` → http://localhost:3000
- ❌ **ЗАПРЕЩЕНЫ:** 3001, 3007, 3010, или любые другие порты
- ⚠️ **Причина:** Множественные порты создают конфликты кэша и старые версии компонентов

**ВСЕГДА используй:** `PORT=3000 npm run dev`

---

## 🎯 ПРОБЛЕМА

При разработке с Next.js 16 + Turbopack возникали следующие проблемы:

1. **Множественные Node процессы** запускались одновременно
2. **Старые Node сессии** продолжали работать в фоне с устаревшим кодом
3. **Поврежденный `.next` кэш** не обновлялся автоматически
4. **Изменения в коде не применялись** из-за агрессивного кэширования Turbopack
5. **Port conflicts** - порт 3000 занят старым процессом

**Симптомы:**
- Console logs показывают старую версию компонента (например, Badge V6.2 вместо V6.6)
- Изменения в коде не отражаются в браузере после перезагрузки
- Ошибка: "Cannot find module '../chunks/ssr/[turbopack]_runtime.js'"
- Ошибка: "Port 3000 is already in use"
- Параметры компонентов не обновляются в real-time (например, V7.6 Typography не применяется)

---

## ✅ РЕШЕНИЕ: Стандартный протокол запуска dev сервера

### 🔴 КРИТИЧНО: ВСЕГДА выполняй ЭТИ шаги перед запуском

```bash
# 1. Найти и убить ВСЕ процессы на порту 3000
lsof -ti:3000 2>/dev/null | xargs -r kill -9 2>/dev/null

# 2. Убить процессы Next.js/Turbopack (по имени)
pkill -9 -f "next-server" 2>/dev/null
pkill -9 -f "turbopack" 2>/dev/null

# 3. Удалить .next кэш (ОБЯЗАТЕЛЬНО!)
rm -rf .next

# 4. Удалить node_modules кэш (опционально, если проблемы остаются)
rm -rf node_modules/.cache

# 5. Подождать 2-3 секунды для полной очистки
sleep 3

# 6. Запустить dev сервер СТРОГО на порту 3000
PORT=3000 npm run dev
```

### 🚀 Одной командой (рекомендуется, БЕЗОПАСНАЯ)

```bash
lsof -ti:3000 2>/dev/null | xargs -r kill -9 2>/dev/null && pkill -9 -f "next-server" 2>/dev/null && pkill -9 -f "turbopack" 2>/dev/null && rm -rf .next && sleep 3 && PORT=3000 npm run dev
```

### ⚠️ СТАРАЯ КОМАНДА (ОПАСНО - НЕ ИСПОЛЬЗУЙ!)

```bash
# ❌ ОПАСНО: Убивает VSCode + Claude Code!
pkill -9 node && fuser -k 3000/tcp 2>/dev/null && rm -rf .next && sleep 3 && npm run dev
```

**Почему опасно:** `pkill -9 node` убивает ВСЕ Node.js процессы, включая VSCode (Electron) и Claude Code CLI

---

## 📋 ПРАВИЛА ДЛЯ CLAUDE AI

### ⚠️ КРИТИЧНО: При работе с dev сервером

1. **СТРОГО запускай ТОЛЬКО на порту 3000:** `PORT=3000 npm run dev`
2. **НИКОГДА не запускай на других портах** (3001, 3007, 3010 и т.д.)
3. **ВСЕГДА убивай ВСЕ старые Node сессии** перед новым запуском
4. **ВСЕГДА удаляй `.next` кэш** перед запуском (ОБЯЗАТЕЛЬНО!)
5. **ВСЕГДА жди 3 секунды** после очистки процессов перед запуском
6. **ПРОВЕРЯЙ что порт 3000 свободен** перед запуском: `lsof -ti:3000`

### 🔧 Когда применять полную очистку

**Полная очистка требуется если:**
- ✅ Внесены изменения в компоненты (особенно React компоненты)
- ✅ Изменены CSS модули
- ✅ Обновлены interfaces/types
- ✅ Console logs показывают старую версию кода
- ✅ Получена ошибка "Cannot find module"
- ✅ Сервер автоматически переключился на другой порт

**Полная очистка НЕ требуется если:**
- ❌ Меняется только содержимое текста (content)
- ❌ Правки в markdown документации
- ❌ Изменения в .env файлах

### 📝 Алгоритм для Claude

**При получении задачи на изменение кода:**

```
1. Вношу изменения в файлы
2. Проверяю: затронуты React компоненты / CSS / types?
   ├─ ДА → Выполняю ПОЛНУЮ ОЧИСТКУ:
   │        lsof -ti:3000 | xargs kill -9 2>/dev/null && \
   │        pkill -9 -f "next-server" 2>/dev/null && \
   │        pkill -9 -f "turbopack" 2>/dev/null && \
   │        rm -rf .next && sleep 3 && PORT=3000 npm run dev
   └─ НЕТ → Простой перезапуск: PORT=3000 npm run dev
3. Жду старта сервера на порту 3000 (проверяю "Ready in Xms")
4. Проверяю console logs на правильную версию компонента
5. НИКОГДА не запускаю на других портах!
```

---

## 🐛 TROUBLESHOOTING

### Проблема 1: "Port 3000 is already in use"

**Причина:** Старая Node сессия уже запущена на порту 3000

**Решение:**
```bash
# Шаг 1: Найти процесс на порту 3000
lsof -ti:3000

# Шаг 2: Убить процесс на порту 3000
lsof -ti:3000 | xargs kill -9 2>/dev/null

# Шаг 3: Убить процессы Next.js/Turbopack
pkill -9 -f "next-server" 2>/dev/null
pkill -9 -f "turbopack" 2>/dev/null

# Шаг 4: Удалить кэш и запустить заново
rm -rf .next && sleep 3 && PORT=3000 npm run dev
```

**Одной командой:**
```bash
lsof -ti:3000 | xargs kill -9 2>/dev/null && pkill -9 -f "next-server" 2>/dev/null && pkill -9 -f "turbopack" 2>/dev/null && rm -rf .next && sleep 3 && PORT=3000 npm run dev
```

---

### Проблема 2: Изменения в коде не применяются

**Причина:** Старые Node сессии + поврежденный Turbopack кэш в `.next`

**Решение:**
```bash
# Полная очистка (БЕЗОПАСНАЯ - не убивает VSCode)
lsof -ti:3000 | xargs kill -9 2>/dev/null
pkill -9 -f "next-server" 2>/dev/null
pkill -9 -f "turbopack" 2>/dev/null
rm -rf .next node_modules/.cache
sleep 3
PORT=3000 npm run dev
```

---

### Проблема 3: Множественные Node процессы

**Проверка:**
```bash
# Посмотреть процессы Next.js
ps aux | grep "next dev"

# Посмотреть что занимает порт 3000
lsof -i:3000
ss -tulpn | grep :3000
```

**Решение:**
```bash
# Убить ВСЕ процессы Next.js/Turbopack (БЕЗОПАСНАЯ команда)
lsof -ti:3000 | xargs kill -9 2>/dev/null
pkill -9 -f "next-server" 2>/dev/null
pkill -9 -f "turbopack" 2>/dev/null

# Проверить что все убиты
lsof -i:3000  # Должно быть пусто

# Удалить кэш и запустить заново
rm -rf .next && sleep 3 && PORT=3000 npm run dev
```

---

### Проблема 4: "Cannot find module '../chunks/ssr/[turbopack]_runtime.js'"

**Причина:** Поврежденный Turbopack кэш в `.next`

**Решение:**
```bash
# Убить процессы + удалить .next полностью
lsof -ti:3000 | xargs kill -9 2>/dev/null
pkill -9 -f "next-server" 2>/dev/null
pkill -9 -f "turbopack" 2>/dev/null
rm -rf .next
sleep 3

# Запустить заново
PORT=3000 npm run dev
```

---

### Проблема 5: Console logs показывают старую версию компонента

**Пример:** Изменил Badge на V7.6, но console показывает "Badge V6.2"

**Причина:** Старые Node сессии работают с устаревшим кодом + браузер кэш

**Решение:**
```bash
# 1. Полная очистка сервера (БЕЗОПАСНАЯ)
lsof -ti:3000 | xargs kill -9 2>/dev/null
pkill -9 -f "next-server" 2>/dev/null
pkill -9 -f "turbopack" 2>/dev/null
rm -rf .next
sleep 3
PORT=3000 npm run dev

# 2. В браузере: Hard Refresh (Ctrl+Shift+R или Cmd+Shift+R)
# 3. ИЛИ открыть DevTools → Network → Disable cache
```

---

## 🔍 ДИАГНОСТИКА

### Как проверить что сервер работает правильно

```bash
# 1. Проверить что только ОДИН Node процесс запущен
ps aux | grep "npm run dev"
# Должен быть только ОДИН процесс

# 2. Проверить порт
lsof -i :3000
# Должен показать npm/node на порту 3000

# 3. Проверить console logs в браузере
# Должны показывать АКТУАЛЬНУЮ версию компонента
```

---

## 📦 NEXT.JS 16 + TURBOPACK ОСОБЕННОСТИ

### Агрессивное кэширование

Next.js 16 с Turbopack использует **агрессивную стратегию кэширования**:

- `.next/cache/` - кэш билда
- `.next/server/` - серверные компоненты
- `node_modules/.cache/` - кэш зависимостей

**Когда Turbopack НЕ обновляет кэш автоматически:**
- Изменения в CSS modules
- Обновления TypeScript interfaces
- Изменения в React компонентах с useRef/useEffect
- Модификации в utility функциях

**Поэтому:** ВСЕГДА удаляй `.next` при изменениях в компонентах!

---

## ⚠️ БЕЗОПАСНЫЕ КОМАНДЫ (НЕ УБИВАЮТ VSCODE)

### 🚨 ВАЖНО: Проблема с `pkill -9 node`

**Команда `pkill -9 node` убивает ВСЕ Node процессы, включая:**
- ❌ VSCode (работает на Electron/Node.js)
- ❌ Claude Code CLI (работает на Node.js)
- ❌ Другие важные приложения
- ✅ npm run dev (нужно убить)

**Результат:** VSCode вылетает, показывает кнопку "Restart", теряется сессия Claude Code!

---

### ✅ БЕЗОПАСНЫЕ АЛЬТЕРНАТИВЫ

#### Вариант 1: По порту 3000 (рекомендуется)

```bash
# Убить только процессы на порту 3000
lsof -ti:3000 2>/dev/null | xargs -r kill -9 2>/dev/null
pkill -9 -f "next-server" 2>/dev/null
pkill -9 -f "turbopack" 2>/dev/null
rm -rf .next
sleep 3
PORT=3000 npm run dev
```

**Одной командой:**
```bash
lsof -ti:3000 2>/dev/null | xargs -r kill -9 2>/dev/null && pkill -9 -f "next-server" 2>/dev/null && pkill -9 -f "turbopack" 2>/dev/null && rm -rf .next && sleep 3 && PORT=3000 npm run dev
```

---

#### Вариант 2: По директории проекта (самый безопасный)

```bash
# Убить только процессы в директории проекта
ps aux | grep "/var/www/bubble-gum" | grep -E "node|next" | grep -v grep | awk '{print $2}' | xargs -r kill -9 2>/dev/null
lsof -ti:3000 2>/dev/null | xargs -r kill -9 2>/dev/null
rm -rf .next
sleep 3
PORT=3000 npm run dev
```

---

#### Вариант 3: По названию процесса

```bash
# Убить только процессы связанные с проектом
pkill -9 -f "bubble-gum.*node" 2>/dev/null
pkill -9 -f "next dev" 2>/dev/null
lsof -ti:3000 2>/dev/null | xargs -r kill -9 2>/dev/null
rm -rf .next
sleep 3
PORT=3000 npm run dev
```

---

### 🎯 Рекомендованные alias для ~/.bashrc

```bash
# Добавить в ~/.bashrc или ~/.zshrc

# Безопасное убийство dev сервера (только порт 3000)
alias kill-dev='lsof -ti:3000 2>/dev/null | xargs -r kill -9 2>/dev/null && pkill -9 -f "next-server" 2>/dev/null && pkill -9 -f "turbopack" 2>/dev/null && sleep 3 && echo "✅ Dev server killed safely"'

# Полная очистка для Bubble Gum
alias kill-bg='lsof -ti:3000 2>/dev/null | xargs -r kill -9 2>/dev/null && pkill -9 -f "bubble-gum.*next" 2>/dev/null && rm -rf .next && sleep 3 && echo "✅ Bubble Gum dev killed + cache cleared"'

# Быстрый рестарт (СТРОГО на порту 3000)
alias restart-dev='kill-dev && PORT=3000 npm run dev'
alias restart-bg='kill-bg && PORT=3000 npm run dev'
```

**Использование:**
```bash
# Убить dev сервер безопасно
kill-dev

# Убить + очистить кэш для Bubble Gum
kill-bg

# Рестарт одной командой (запустится на порту 3000)
restart-bg
```

---

### 🚫 ОПАСНЫЕ КОМАНДЫ (НИКОГДА НЕ ИСПОЛЬЗУЙ)

```bash
# ❌ ОПАСНО: Убивает VSCode + Claude Code!
pkill -9 node

# ❌ ОПАСНО: Убивает ВСЕ Node процессы включая VSCode!
ps aux | grep -E "npm|node|next" | grep -v grep | awk '{print $2}' | xargs -r kill -9

# ❌ ОПАСНО: Убивает все npm процессы!
pkill -9 -f "npm run dev"
```

**Почему опасно:**
- VSCode основан на Electron (Node.js/Chrome)
- Claude Code CLI работает на Node.js
- Эти команды убивают ВСЕ Node процессы без разбора
- Результат: Потеря сессии, нужен Restart VSCode

---

## 🎓 BEST PRACTICES

### ✅ DO (Правильно)

```bash
# ✅ Безопасное убийство по порту 3000 + очистка кэша
lsof -ti:3000 | xargs kill -9 2>/dev/null && rm -rf .next && sleep 3 && PORT=3000 npm run dev

# ✅ Использовать alias
restart-bg  # (после создания alias)

# ✅ СТРОГО запускать ТОЛЬКО на порту 3000
PORT=3000 npm run dev

# ✅ Ждать 3 секунды после kill
lsof -ti:3000 | xargs kill -9 && sleep 3 && PORT=3000 npm run dev

# ✅ Проверять версию в console logs
console.log('Badge V7.6')  # Указывай версию в логах

# ✅ Убивать старые сессии перед запуском
pkill -9 -f "next-server" && pkill -9 -f "turbopack"
```

### ❌ DON'T (Неправильно)

```bash
# ❌ Убивать ВСЕ Node процессы (убьёт VSCode!)
pkill -9 node

# ❌ Запускать без очистки кэша после изменений
npm run dev  # (после изменений в компонентах - ОПАСНО!)

# ❌ Запускать на других портах (ЗАПРЕЩЕНО!)
npm run dev  # Без PORT=3000
PORT=3001 npm run dev  # НЕТ!
PORT=3007 npm run dev  # НЕТ!
PORT=3010 npm run dev  # НЕТ!

# ❌ Не ждать после kill
lsof -ti:3000 | xargs kill -9 && PORT=3000 npm run dev  # Слишком быстро!

# ❌ Не удалять .next кэш после изменений
lsof -ti:3000 | xargs kill -9 && PORT=3000 npm run dev  # Кэш остался!
```

---

## 🔗 СВЯЗАННЫЕ ДОКУМЕНТЫ

- [CANVAS_STORE_PROPS_CLEANING.md](./CANVAS_STORE_PROPS_CLEANING.md) - Проблема которая была обнаружена через server issues
- [TECH_STACK.md](./TECH_STACK.md) - Next.js 16 + Turbopack конфигурация
- [GOD_TIER_PROTOCOL.md](./GOD_TIER_PROTOCOL.md) - Стандарты разработки

---

## 📊 ИСТОРИЯ ИЗМЕНЕНИЙ

### Version 1.2.0 (November 11, 2025)

**🚨 Критичное обновление: ТОЛЬКО ПОРТ 3000**

**Проблема:**
- Множественные старые Node сессии работали в фоне с устаревшим кодом
- V7.6 параметры Badge не обновлялись в real-time на canvas
- Изменения в компонентах не применялись из-за старых сессий

**Изменения:**
- ✅ Добавлено требование: **СТРОГО запускать ТОЛЬКО на порту 3000**
- ✅ Все команды обновлены с `PORT=3000 npm run dev`
- ✅ Убраны упоминания портов 3007, 3010 (запрещены)
- ✅ Увеличена задержка с 2 до 3 секунд после kill
- ✅ Добавлено: убивать старые сессии `pkill -9 -f "next-server"`, `pkill -9 -f "turbopack"`
- ✅ Обновлены alias с PORT=3000
- ✅ Добавлено требование проверки порта перед запуском: `lsof -ti:3000`
- ✅ Добавлен симптом: "Параметры компонентов не обновляются в real-time"

**Причина обновления:**
- Пользователь столкнулся с проблемой: изменения в Badge V7.6 не применялись
- Причина: множественные старые Node сессии работали в фоне
- Решение: ВСЕГДА убивать ВСЕ старые сессии + запускать ТОЛЬКО на порту 3000

---

### Version 1.1.0 (November 10, 2025)

**🚨 Критичное обновление:**
- ✅ Добавлен раздел "БЕЗОПАСНЫЕ КОМАНДЫ (НЕ УБИВАЮТ VSCODE)"
- ✅ Объяснена проблема с `pkill -9 node` (убивает VSCode + Claude Code)
- ✅ Предоставлены 3 безопасные альтернативы:
  - По портам (рекомендуется)
  - По директории проекта (самый безопасный)
  - По названию процесса
- ✅ Добавлены рекомендованные alias для ~/.bashrc
- ✅ Обновлены все команды в документе на безопасные версии
- ✅ Добавлен список ОПАСНЫХ команд с объяснениями

**Причина обновления:**
- Пользователь столкнулся с проблемой: команда `pkill -9 node` убивала VSCode
- VSCode вылетал, показывал кнопку "Restart", терялась сессия Claude Code
- Нужны были команды, которые убивают ТОЛЬКО dev сервер, не трогая IDE

---

### Version 1.0.0 (November 10, 2025)

**Причина создания:**
- Множественные проблемы с dev сервером во время Badge V6.x разработки
- Node процессы на портах 3000, 3007, 3010 одновременно
- Turbopack кэш не обновлялся после изменений
- Console logs показывали старые версии компонентов

**Решение:**
- Создан стандартный протокол запуска dev сервера
- Правила для Claude AI по управлению сессиями
- Troubleshooting guide для распространенных проблем

---

## 🎯 SUMMARY

**ЗОЛОТОЕ ПРАВИЛО (БЕЗОПАСНОЕ):**

```bash
# Перед КАЖДЫМ запуском после изменений в компонентах:
lsof -ti:3000 | xargs kill -9 2>/dev/null && pkill -9 -f "next-server" 2>/dev/null && pkill -9 -f "turbopack" 2>/dev/null && rm -rf .next && sleep 3 && PORT=3000 npm run dev
```

**ИЛИ используй alias:**
```bash
# После создания alias в ~/.bashrc
restart-bg
```

**ВСЕГДА:**
- ✅ **СТРОГО запускай ТОЛЬКО на порту 3000:** `PORT=3000 npm run dev`
- ✅ Убивай ТОЛЬКО dev процессы (по порту 3000)
- ✅ Убивай старые сессии: `pkill -9 -f "next-server"`, `pkill -9 -f "turbopack"`
- ✅ НЕ используй `pkill -9 node` (убьёт VSCode!)
- ✅ ОБЯЗАТЕЛЬНО удаляй `.next` кэш после изменений
- ✅ Жди 3 секунды после kill
- ✅ Проверяй версию в console logs

**НИКОГДА:**
- ❌ **НЕ запускай на других портах!** (3001, 3007, 3010 и т.д.)
- ❌ Не используй `pkill -9 node` (убьёт VSCode + Claude Code!)
- ❌ Не создавай множественные сессии
- ❌ Не пропускай очистку .next кэша после изменений
- ❌ Не запускай сразу после kill (жди 3 сек)

---

**Last Updated:** November 11, 2025
**Version:** 1.2.0
**Maintained by:** Claude AI
**Status:** ✅ Active - Use for all dev server operations

**🚨 ВАЖНО:** ВСЕГДА запускай dev сервер СТРОГО на порту 3000: `PORT=3000 npm run dev`

**⚠️ ЗАПРЕЩЕНО:** Запуск на других портах (3001, 3007, 3010 и т.д.) создаёт конфликты кэша!
